import { ThemeExtension } from "themes";
import Progress from "./progress";
import FormLabel from "./formLabel";
import Input from "./input";

const components: ThemeExtension["components"] = {
  Progress,
  FormLabel,
  Input,
};

export default components;
